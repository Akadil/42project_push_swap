def ft_cost_function(stack a, stack b):
    while (b.size != 0)
    {
        minimal = 0;
        minimal_pos = 0;

        costs = malloc(b.size + 1)
        for item in b:
            costs[i] = ft_cost(a, b, item)
            if costs[i] < minimal:
                minimal = costs[i]
                minimal_pos = i
        ft_do_opearation(minimal_ind, a, b)
        free (costs)
    }